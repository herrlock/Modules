
apply from: new File(rootProject.projectDir, 'gradle/java.gradle')

apply plugin: 'eclipse-wtp'
apply plugin: 'war'

dependencies {
	runtime 'org.apache.logging.log4j:log4j-web:2.5'
}

task rawWar(type: War) {
	archiveName = "${project.name}.war"
}
assemble.dependsOn rawWar
